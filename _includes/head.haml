---
---
%head
  %meta{charset: "utf-8"}
  %title Marc Ziani de Ferranti
  %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}
  %meta{content: "", name: "description"}
  %link{:href => "{{ site.url }}/feed.xml", :rel => "alternate", :title => "{{ site.name }}", :type => "application/rss+xml"}/
  %link{href: "/assets/favicon.ico?v=2", :rel => "shortcut icon"}

  %script{src: "/assets/js/blazy.min.js", type: "text/javascript"}
  :javascript
    var bLazy = new Blazy({
    // Options
    });

  %link{rel:"preload", href:"http://a.marcziani.com/fonts/Copernicus-Book.woff"}
  %link{rel:"preload", href:"http://a.marcziani.com/fonts/KlavikaWebBasicRegular.woff"}

  %link{href: "/assets/screen.min.css", rel: "stylesheet"}

  -# %script{src: "/assets/javascripts/fontfaceobserver.js", type: "text/javascript"}
  %script{src: "/assets/js/fontloader.min.js", type: "text/javascript"}

  -# :javascript
    /* include loadJS library */
    (function( w ){
      var loadJS = function( src, cb ){
        "use strict";
        var ref = w.document.getElementsByTagName( "script" )[ 0 ];
        var script = w.document.createElement( "script" );
        script.src = src;
        script.async = true;
        ref.parentNode.insertBefore( script, ref );
        if (cb && typeof(cb) === "function") {
          script.onload = cb;
        }
        return script;
      };
      // commonjs
      if( typeof module !== "undefined" ){
        module.exports = loadJS;
      } else {
        w.loadJS = loadJS;
      }
    }( typeof global !== "undefined" ? global : this ));

  / HTML5 shim, for IE6-8 support of HTML5 elements
  /[if lt IE 9]
    %script{src: "http://html5shim.googlecode.com/svn/trunk/html5.js"}
