== ---
title: Hello, World!
layout: listing
== ---

%article.writing
  %section
    %ul
      == {% for post in site.categories.writing %}
      %li
        %time {{ post.date | date: "%b %Y" }}
        %a{ href: "{{ post.url }}" }
          == {{ post.title }}
      == {% endfor %}
