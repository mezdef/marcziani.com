== ---
title: Hello, World!
layout: listing
== ---

%article.listing
  %section
    %ul
      == {% for post in site.categories.writing %}
      %li
        %a{ href: "{{ post.url }}" }
          == {{ post.title }}
        %time {{ post.date | date: "%b %Y" }}
      == {% endfor %}
