== ---
title: Hello, World!
layout: default
== ---

%section.listing
  == {% for post in site.posts %}
  %article
    %a{ href: "{{ post.url }}" }
      == {{ post.title }}
    %time {{ post.date | date: "%b %Y" }}
  == {% endfor %}