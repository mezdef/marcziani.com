== ---
title: Marc Ziani de Ferranti // Projects
layout: listing
permalink: projects/
== ---

%article.projects.listing
  %section
    %ul
      == {% for post in site.categories.projects %}
      %li
        %a{ href: "{{ post.url }}" }
          .about
            %h1 {{ post.title }}
            %span {{ post.subtitle }}
          %img{ src: "{{ post.thumbnail }}"}
      == {% endfor %}
