== ---
title: Marc Ziani de Ferranti // Photos
layout: listing
permalink: photos/
== ---

%article.photos.listing
  %section
    %ul
      == {% for album in site.categories.photos %}
      %li
        %a{ href: "{{ album.url }}" }
          %span {{ album.title }}
        %time {{ album.date | date: "%b %Y" }}
      == {% endfor %}
