== ---
title: Marc Ziani de Ferranti // Photos
layout: listing
permalink: photos/
== ---

%article.photos
  %section
    %ul.cols3
      == {% for album in site.categories.photos %}
      %li.one-third
        %a{ href: "{{ album.url }}" }
          %img{ src: "{{ album.thumbnail }}"}
          %span {{ album.title }}
        %nav
          %time {{ album.date | date: "%Y" }}
          %span,
          == {% for tag in album.tags %}
          %a{ href: "{{ tag.url }}"} {{ tag }}
          == {% endfor %}
      == {% endfor %}
